# 🏢 Корпоративный портал - Wiki Creator

Скрипт для автоматического создания структурированной wiki в Notion для разработки корпоративного портала.

## 🚀 Быстрый старт

### 1. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 2. Настройка Notion API

#### Получение токена интеграции:
1. Перейдите на https://www.notion.so/my-integrations
2. Создайте новую интеграцию
3. Скопируйте токен

#### Получение ID страницы:
1. Откройте страницу в Notion, где хотите создать wiki
2. Скопируйте ID из URL: `https://notion.so/YOUR_PAGE_ID`

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта:
```bash
cp env_example.txt .env
```

Заполните переменные:
```
NOTION_TOKEN=your_notion_integration_token_here
PARENT_PAGE_ID=your_page_id_here
```

### 4. Предоставление доступа

В Notion:
1. Откройте страницу, где хотите создать wiki
2. Нажмите "Share" → "Invite" → выберите вашу интеграцию

### 5. Запуск скрипта
```bash
python create_notion_wiki.py
```

## 📁 Структура wiki

Скрипт создаст следующую структуру:

```
📖 Wiki Корпоративного портала
├── 🎯 Product Vision & Roadmap
│   └── 📝 User Stories (база данных)
├── 🏗️ Техническая документация
│   ├── 🏛️ Архитектура системы
│   ├── 📚 База знаний
│   ├── 📊 Дашборды
│   ├── 🤖 AI-агенты
│   └── 🔌 API & Интеграции
├── 🎨 Дизайн система
│   ├── 🖌️ Брендинг
│   ├── 🧩 UI компоненты
│   ├── 📱 Responsive дизайн
│   ├── ♿ Доступность
│   └── 📐 Прототипы
├── 💻 Development Guidelines
│   ├── 📝 Code Style
│   ├── 🔄 Git Workflow
│   ├── 🧪 Testing Strategy
│   ├── 👁️ Code Review
│   └── 📚 Documentation
├── 🧪 Testing & QA
│   ├── 📋 Test Cases
│   ├── 🤖 Автоматизация
│   ├── ⚡ Performance
│   ├── 🔒 Security
│   └── ✅ UAT
├── 🚀 Deployment & Operations
│   ├── ☁️ Infrastructure
│   ├── 🔄 CI/CD Pipeline
│   ├── 📊 Monitoring
│   ├── 💾 Backup
│   └── 📈 Scaling
├── 👥 Team Resources
│   ├── 📝 Meeting Notes
│   ├── 📋 Decision Log
│   ├── 🆕 Onboarding
│   ├── 📖 Knowledge Sharing
│   └── 🛠️ Tools & Licenses
├── 📊 Analytics & Metrics
│   ├── 📈 Product Metrics
│   ├── 💻 Development Metrics
│   ├── 💼 Business Metrics
│   └── 📋 Dashboards
├── 🔧 Troubleshooting & Support
│   ├── ❗ Common Issues
│   ├── 🐛 Debugging
│   ├── 🎧 Support Process
│   └── 🚨 Incident Response
└── 📚 Resources & References
    ├── 📖 External Docs
    ├── 📄 Research Papers
    ├── 🏛️ Industry Standards
    └── 🎓 Training Materials
```

## ⚙️ Кастомизация

Вы можете модифицировать скрипт для:
- Изменения структуры страниц
- Добавления новых разделов
- Кастомизации контента
- Изменения иконок и названий

## 🔧 Troubleshooting

### Ошибка аутентификации
- Проверьте правильность `NOTION_TOKEN`
- Убедитесь, что интеграция активна

### Ошибка доступа к странице
- Проверьте `PARENT_PAGE_ID`
- Убедитесь, что интеграция имеет доступ к странице

### Ошибка создания страниц
- Проверьте лимиты Notion API (3 запроса в секунду)
- Убедитесь в наличии места в воркспейсе

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи выполнения скрипта
2. Убедитесь в правильности настроек
3. Проверьте статус Notion API

## 📋 Требования

- Python 3.7+
- Notion API токен с правами на создание страниц
- Доступ к целевой странице в Notion

## 📝 Опрос (веб-приложение)

В проект добавлено минималистичное веб-приложение для проведения опроса с таймером на 25 минут, кнопкой завершения и сохранением ответов в базу данных SQLite.

### Запуск локально
```bash
pip install -r requirements.txt
python -m uvicorn app:app --host 0.0.0.0 --port 8000
```

Откройте в браузере `http://localhost:8000`. Ответы сохраняются в файл `survey.db`.

### Настройка приветствия
Измените приветственный текст в `web/index.html` (элемент `#welcome-text`).

### Экспорт результатов в Excel
Откройте в браузере `https://ВАШ_ДОМЕН/admin` и нажмите «Скачать результаты».
Файл в формате `.xlsx` скачается автоматически.

### Деплой
Для продакшн-развертывания:
```bash
python -m uvicorn app:app --host 0.0.0.0 --port 8000 --workers 2
```
Обычно приложение проксируют через Nginx/Apache и закрепляют домен на 80/443.

## 🧭 Подробная инструкция деплоя (для новичка)

Ниже пример деплоя на VPS с Ubuntu 22.04. Если у вас другой хостинг, шаги будут похожими.

### 1. Купите сервер (VPS)
- Выберите любого провайдера и создайте VPS (например, 1 CPU, 1–2 GB RAM).
- Получите IP‑адрес сервера и данные для входа по SSH.

### 2. Привяжите домен
- В панели домена добавьте A‑запись:
  - `@` → IP вашего сервера
  - `www` → IP вашего сервера (по желанию)
- Подождите 5–30 минут, пока запись обновится.

### 3. Подключитесь к серверу
На Windows можно использовать PowerShell:
```bash
ssh root@ВАШ_IP
```
Введите пароль, выданный провайдером.

### 4. Установите зависимости на сервере
```bash
apt update && apt upgrade -y
apt install -y python3 python3-venv python3-pip nginx git
```

### 5. Загрузите проект на сервер
Вариант 1: через Git (если репозиторий на GitHub/GitLab):
```bash
git clone https://github.com/ВАШ_АККАУНТ/ВАШ_РЕПО.git survey
```

Вариант 2: через ZIP (если проекта нет в репозитории):
- Упакуйте папку проекта в ZIP и загрузите на сервер любым способом.

### 6. Создайте виртуальное окружение и установите зависимости
```bash
cd survey
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

### 7. Запустите сервис через systemd
Создайте файл сервиса:
```bash
nano /etc/systemd/system/survey.service
```
Вставьте содержимое (замените путь к проекту):
```
[Unit]
Description=Company Survey App
After=network.target

[Service]
User=root
WorkingDirectory=/root/survey
ExecStart=/root/survey/.venv/bin/python -m uvicorn app:app --host 127.0.0.1 --port 8000
Restart=always

[Install]
WantedBy=multi-user.target
```
Сохраните и активируйте сервис:
```bash
systemctl daemon-reload
systemctl enable survey
systemctl start survey
systemctl status survey
```

### 8. Настройте Nginx (домен → приложение)
Создайте конфиг:
```bash
nano /etc/nginx/sites-available/survey
```
Вставьте (замените домен):
```
server {
    listen 80;
    server_name ваш-домен.ru www.ваш-домен.ru;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```
Активируйте сайт:
```bash
ln -s /etc/nginx/sites-available/survey /etc/nginx/sites-enabled/
nginx -t
systemctl reload nginx
```

### 9. Выпустите SSL‑сертификат (HTTPS)
```bash
apt install -y certbot python3-certbot-nginx
certbot --nginx -d ваш-домен.ru -d www.ваш-домен.ru
```
Certbot сам настроит HTTPS и автообновление.

### 10. Проверка
- Откройте `https://ваш-домен.ru` — опрос.
- Откройте `https://ваш-домен.ru/admin` — экспорт Excel.

### Полезные команды
Посмотреть логи:
```bash
journalctl -u survey -f
```
Перезапуск сервиса:
```bash
systemctl restart survey
```